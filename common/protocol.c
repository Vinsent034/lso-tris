#include "protocol.h"
#include <stdio.h>

void send_packet(int sockfd, Packet *packet) {
    // TODO: implementare domani
    (void)sockfd;
    (void)packet;
}

void *serialize_packet(Packet *packet) {
    // TODO: implementare domani
    (void)packet;
    return NULL;
}